import React from 'react';

const Footer = () => {
    console.log('Footer');
    return (
        <div>
            <h1>This is footer</h1>
        </div>
    );
};

export default React.memo(Footer);